name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - numpy
  - pandas
  - scipy
  - matplotlib
  - sys 

  # Pip-only installs
  - pip:
    - codecov

